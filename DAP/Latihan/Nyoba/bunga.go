package main
import "fmt"
func main() {
    var n int

    fmt.Print("N: ")
    fmt.Scanln(&n)
    arr := make([n]string)

    for i := 0;i < n;i++ {
      fmt.Printf("Bunga: ")
      fmt.Scanln(&arr[n])
    }
}
